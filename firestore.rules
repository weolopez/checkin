rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read: if request.auth != null; // Allow read access to all authenticated users
      allow write: if request.auth != null && request.auth.uid == userId; // Allow write access only to the user who owns the document
    }
  }
}
